;;;;;;;;;;;;;;;;;;;;;
; FPM Configuration ;
;;;;;;;;;;;;;;;;;;;;;

pid = /var/run/php5-fpm.pid
daemonize = no

[www]
user   = phpfpm
listen = 127.0.0.1:9000
listen.mode  = 0000
pm = ondemand
pm.max_children      = 5
pm.start_servers     = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
